#include <stdio.h>
#include <string.h>

int main(void) {
  char str[] = "Hello, World!";
  int length = strlen(str);
  int i=10;

  printf("String: %s\n", str);
  printf("Length: %d\n", length);
  printf("i=%d",i);
  return 0;
}
